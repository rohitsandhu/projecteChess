{# {{ form_start(form) }} #}
{# {{ form_widget(form) }} #}
{# <button class="btn">{{ button_label|default('Save') }}</button> #}
{# {{ form_end(form) }} #}


{{ form_start(form) }}
    <div class="my-custom-class-for-errors">
        {{ form_errors(form) }}
    </div>


    <div class="container align-middle divLogin divCrearTorneig ">
        <div class="row customLoginRow">
            <div class="col-lg-8 loginFormCustom p-2    ">

                <div class="form-group mt-3">
                    <div class="col-12">
                        {{ form_row(form.nom) }}
                    </div>
                </div>

                <div class="form-group">
                    <div class="col-12">
                        {{ form_row(form.cognoms) }}
                    </div>
                </div>

                <div class="form-group">
                    <div class="col-12">
                        {{ form_row(form.elo) }}
                    </div>
                </div>

                <div class="form-group">
                    <div class="col-12">
                        {{ form_row(form.pais) }}
                    </div>
                </div>

                <div class="form-group">
                    <div class="col-12">
                        {{ form_row(form.club) }}
                    </div>
                </div>

                <div class="form-group">
                    <div class="col-12">
                        {{ form_row(form.dni) }}
                    </div>
                </div>

                <div class="form-group">
                    <div class="col-12">
                        {{ form_row(form.dataNaixament) }}
                    </div>
                </div>
                <button class="btn btn-light">{{ button_label|default('Save') }}</button>
            </div>
        </div>
    </div>

{{ form_end(form) }}

