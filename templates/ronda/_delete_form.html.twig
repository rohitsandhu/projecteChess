<form method="post" action="{{ path('ronda_delete', {'id': ronda.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ronda.id) }}">
    <button class="btn">Delete</button>
</form>
