{% extends 'base.html.twig' %}

{% block title %}Arbitre{% endblock %}

{% block body %}
    <h1>Arbitre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ arbitre.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ arbitre.nom }}</td>
            </tr>
            <tr>
                <th>Cognoms</th>
                <td>{{ arbitre.cognoms }}</td>
            </tr>
            <tr>
                <th>Pais</th>
                <td>{{ arbitre.pais }}</td>
            </tr>
            <tr>
                <th>Usuari</th>
                <td>{{ arbitre.usuari }}</td>
            </tr>
            <tr>
                <th>Contrasenya</th>
                <td>{{ arbitre.contrasenya }}</td>
            </tr>
            <tr>
                <th>Dni</th>
                <td>{{ arbitre.dni }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('arbitre_index') }}">back to list</a>

    <a href="{{ path('arbitre_edit', {'id': arbitre.id}) }}">edit</a>

    {{ include('arbitre/_delete_form.html.twig') }}
{% endblock %}
